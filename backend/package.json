{
  "name": "trip-tally-backend",
  "version": "1.0.0",
  "description": "Trip Tally serverless backend",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/functions/*.ts --bundle --platform=node --target=node20 --outdir=dist/functions --external:pg-native",
    "build:watch": "tsc --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "dev": "ts-node --transpile-only src/local-server.ts",
    "dev:dynamo": "DB_BACKEND=dynamodb DYNAMODB_ENDPOINT=http://localhost:8000 ts-node --transpile-only src/local-server.ts",
    "setup:dynamo-local": "ts-node --transpile-only scripts/setup-dynamo-local.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1026.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/client-secrets-manager": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.1026.0",
    "@aws-sdk/s3-request-presigner": "^3.600.0",
    "dotenv": "^16.4.5",
    "pg": "^8.12.0",
    "uuid": "^9.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.143",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.6",
    "@types/uuid": "^9.0.8",
    "esbuild": "^0.21.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/__tests__/**/*.ts",
      "**/*.test.ts"
    ]
  }
}
